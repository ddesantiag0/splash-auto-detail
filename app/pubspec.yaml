name: splash_auto_app
description: Cross-platform customer and operations app for Splash Auto Detail.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: 1.6.0
  web_socket_channel: 3.0.3
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/splash-auto-logo.png
