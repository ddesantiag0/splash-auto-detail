{
  "name": "splash-auto-detail-site",
  "version": "2.1.0",
  "private": true,
  "description": "Responsive static website for Splash Auto Detail in Chula Vista.",
  "scripts": {
    "test": "node --test",
    "build:availability": "esbuild availability/customer.js --bundle --minify --format=iife --outfile=availability/customer.bundle.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.3.14",
    "esbuild": "0.25.12"
  }
}
